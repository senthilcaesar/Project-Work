import torch

# These 1 dimensional tensor behaves very much like vectors
t_one = torch.tensor([1, 2, 3])
t_two = torch.tensor([1, 2, 3])

# 5 equally spaced numbers between 0 10 10
lin_space = torch.linspace(0, 10, 5)
